class Student {
  constructor(name, slack, tags, slots, profile) {
    this.name = name;
    this.slack = slack;
    this.tags = tags;
    this.slots = slots;
    this.profile = profile;
  }
}

export default Student
